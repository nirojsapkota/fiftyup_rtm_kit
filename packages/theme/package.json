{
  "name": "@rtm-test/theme",
  "version": "1.0.1-rc.1",
  "description": "Stub description for theme",
  "private": false,
  "main": "build/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore **/__tests__,**/*.test.js",
    "watch": "babel -w src --out-dir build --ignore **/__tests__,**/*.test.js",
    "prepare": "cross-env NODE_ENV=production npm run build"
  },
  "author": "RevTech Media",
  "license": "UNLICENSED",
  "devDependencies": {
    "@babel/cli": "^7.1.0",
    "cross-env": "^5.1.4"
  }
}
