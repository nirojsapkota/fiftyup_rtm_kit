{
  "name": "@rtm-test/tracker",
  "version": "1.0.1-rc.2",
  "description": "Stub description for tracker",
  "main": "build/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore **/__tests__,**/*.test.js",
    "watch": "babel -w src --out-dir build --ignore **/__tests__,**/*.test.js",
    "prepare": "cross-env NODE_ENV=production npm run build"
  },
  "author": "RevTech Media",
  "license": "UNLICENSED",
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "logrocket": "^0.6.16",
    "universal-cookie": "^3.0.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "cross-env": "^5.0.5"
  }
}
