import React from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

import { Box } from '@rtm-ui/layout';

const FooterLogo = () => {
  return (
    <Box>
      <Box>This contain footer logo</Box>
    </Box>
  );
};

export default FooterLogo;
