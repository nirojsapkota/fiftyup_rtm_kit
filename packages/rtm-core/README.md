## RTM Core

Builds docker image for the rest of the builds.

### Node Version

Currently uses node v12.13.0 (you can find it in the Dockerfile)

## Version Bump

To release a bump version, Run `npm publish` in your local and push it to master branch
