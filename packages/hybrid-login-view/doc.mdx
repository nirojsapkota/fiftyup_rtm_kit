---
name: HybridLoginView
route: /pages/hybrid-login-view
menu: Pages
---

import { Playground, PropsTable } from 'docz';
import { HybridLoginView, LandingPage } from './src';
import hybridLoginViewProps from './src/__fixtures__/hybridLoginView.js';
import hybridLoginViewWCalc from './src/__fixtures__/hybridLoginViewWCalc.js';
import { DocWrapper } from '../bootstrap/setup/docWrapper';

## Hybrid Login View (landing page)

<DocWrapper>
  <HybridLoginView {...hybridLoginViewProps} />
</DocWrapper>

##### Note : Props "how" denotes the old "How it works" Component and the prop "Why" denotes the new "Why Join Component"

## PropsTable
<PropsTable of={HybridLoginView} />

## Landing Page View (landing page with background video)

<DocWrapper>
  <LandingPage {...hybridLoginViewProps} />
</DocWrapper>

## Landing Page View (static hero image)

<DocWrapper>
  <LandingPage {...hybridLoginViewProps} enableVideoHero={false} />
</DocWrapper>

## Landing Page View (landing page with image hero background)
<DocWrapper>
  <LandingPage {...hybridLoginViewProps} enableImageHeroBg={true} />
</DocWrapper>

## PropsTable

<PropsTable of={LandingPage} />

